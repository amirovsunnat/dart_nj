// import 'class/avtomobil.dart';

void main(List<String> args) {
  // Avtomobil avtomobil = Avtomobil("Malibu");

  // List<String> ismlar = [
  //   "Joey",
  //   "Rachael",
  // ];
  // ismlar.forEach(
  //   (ism) => print(ism.startsWith("R")),
  // );

  // list of numbers (sonlar ro'yxati)
  // List<int> sonlar = [12, 42, 21, -21];
  // double sum = 0;
  // // for eachda
  // sonlar.forEach(
  //   (son) => sum = sum + son,
  // );

  // print(sum);

  // // for
  // double total = 0;
  // for (int son in sonlar) {
  //   total = total + son;
  // }
  // print(total);

  // // tayyor method
  // print(sonlar.reduce(
  //   (total, son) => total + son,
  // ));
}
